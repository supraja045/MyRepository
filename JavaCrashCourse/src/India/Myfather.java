package India;

public class Myfather  
{

	protected int money=10000;
	
	public int properties= 50000;
	
	public static void main(String[] args) {
		Myfather myDad = new Myfather();
		
	}
	
	
	/* 4 Access modifiers 
	 * 		1. Private -> only from that class you can access the variables,
	 * 		2. Default -> from same class ,from same package we can access the variables
	 * 		3. Protected -> from same class, same package and for other package we can access the variables,
	 * 						for which the class should be having extends keyword
	 * 		4. Public -> from same class, same package and other package , we can access the variables.
	 */
}
